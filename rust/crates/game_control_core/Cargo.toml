[package]
name = "game_control_core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
crate-type = ["rlib", "staticlib", "cdylib"]

[dependencies]
game_core_protocols = { path = "../game_core_protocols" }

[lints]
workspace = true
