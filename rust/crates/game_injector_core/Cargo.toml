[package]
name = "game_injector_core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]
game_core_common = { path = "../game_core_common" }
game_core_protocols = { path = "../game_core_protocols" }

[lints]
workspace = true
