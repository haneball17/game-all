
---

# ⚔️ game-all 完整工程合并方案

## 🎯 项目目标

构建一个名为 `game-all` 的解决方案，包含以下三个核心组件：

1. **Payload (game-payload.dll)** : x86 DLL，合并了游戏辅助 (Helper) 和同步器 (Sync) 的核心逻辑。
2. **Injector (game-injector.exe)** : x86 控制台程序，采用 APC 技术将 DLL 注入游戏。
3. **MasterGUI (game-master.exe)** : C# WPF 程序，提供双功能控制面板。

---

## ✅ 工程落地决策（已确认）

1. **协议单一来源方式**：不引入自动生成体系，采用 **Shared/Protocols/** 作为协议“单一来源”。
   - 目标：减少 C++/C# 结构体不一致风险，确保版本一致性。
   - 要求：所有共享内存结构体定义与版本号必须集中维护在 `Shared/Protocols/`。
2. **MasterGUI 与 Injector 关系**：采用**半集成**模式。
   - MasterGUI 仅提供“快捷入口（打开注入器目录/按钮）”与“状态检测提示”。
   - MasterGUI **不直接负责**启动/停止注入器逻辑，降低权限与耦合风险。
3. **GUI 目标框架统一**：所有 GUI 项目统一为 **net8.0-windows**。
   - 目标：减少运行时差异与发布复杂度。
   - 要求：统一 `TargetFramework`，并同步更新相关依赖。

## ✅ 近期决策（2026-02-03~2026-04-01）
1. **Sync 模块回退到 V2（Payload + 控制端/配置）**：
   - 回退同步核心逻辑，同时控制端与配置同步回退到 V2。
   - 保留现有配置读取与日志路径。
   - 先恢复稳定性，再逐项优化（优化需可开关）。
   - 落地方案见：`docs/SyncMod-V2回退方案.md`
2. **Helper 吸怪/聚物拆分与协议升级（2026-02-04）**：
   - 吸怪/聚物逻辑拆分，聚物仅作用于物品对象，默认热键 `\`（VK=0xDC）。
   - HelperStatus 升级至 V4（新增聚物状态字段）。
   - HelperControl 升级至 V3（新增聚物动作字段与 ActionMask 位）。
   - GUI 新增聚物控制与状态展示，热键配置新增聚物项。
3. **动态倍攻/怪物零伤与协议升级（2026-02-04）**：
   - Hook 伤害计算，支持倍攻与怪物零伤开关。
   - HelperStatus 升级至 V5（新增倍攻/无敌状态字段）。
   - HelperControl 升级至 V4（新增倍攻/无敌动作字段与 ActionMask 位）。
   - GUI 增加倍攻滑块+输入框+开关，无敌独立开关。
4. **Helper 默认启动值调整（2026-03-14）**：
   - 全屏攻击、聚物、动态倍攻、怪物零伤默认启动即开启。
   - 动态倍攻默认倍率调整为 20。
   - 同步更新 Payload 内置默认值、自动生成的 `game_helper.ini`、仓库模板与 GUI 参数默认值。
5. **实机问题分析记录（2026-04-01）**：
   - 记录 Sync 实机运行中的两个主要问题：同步轻微延迟/偶发不同步、方向键高速操作后后台窗口卡方向。
   - 初步判断控制端采集线程模型、50ms 心跳补偿、执行端 RawInput/DirectInput 边沿收敛为主要风险点。
   - 详细分析见：`docs/实机问题分析—同步延迟与方向键卡住.md`

6. **Rust 核心重构第一批落地（2026-04-01）**：
   - 新增 `rust/` workspace，开始承接原生核心重构。
   - 首批落地 `game_core_protocols`、`game_core_common`、`game_injector_core`、`game_payload_core` 四个 crate。
   - 已完成协议镜像、配置模型、最小 C ABI 边界与方向键释放收敛状态机。
   - 详细记录见：`docs/Rust核心重构实施记录.md`

7. **Rust 完整重构计划落地（2026-04-01）**：
   - 基于 `docs/DNF2012台湾服与多客户端同步重设计调研.md` 与 `docs/实机问题分析—同步延迟与方向键卡住.md`，整理出完整 Rust 重构路线图。
   - 明确保留“共享内存 + 注入执行”总架构，分阶段推进 Injector、Payload 状态层、输入适配层、控制端采集内核与诊断体系 Rust 化。
   - 详细计划见：`docs/Rust完整重构落地计划.md`
8. **Injector 接入 Rust 契约与默认配置（2026-04-01）**：
   - `Injector` 已开始链接 `game_injector_core` 静态库，并通过 Rust 提供默认配置模板、数值默认视图与 `HelperStatus` 协议契约。
   - 当前仍保留 C++ 主流程，属于阶段 1 的“低风险接入”，为后续迁移配置解析、等待流程、注入状态机做准备。
   - 详细记录见：`docs/Rust核心重构实施记录.md`
9. **Payload 路径级决策继续收口到 Rust（2026-04-01）**：
   - `game_payload_core` 新增 `PathDecision` 与 `payload_core_evaluate_path_decision_header(...)`。
   - `ShouldSpoofFocus` 与 RawInput 路径中的 Mapping 进入条件开始统一走 Rust 路径级决策。
   - 当前属于阶段 3 的继续推进：从“单键决策 Rust 化”扩展到“路径决策 Rust 化”。
   - 详细记录见：`docs/Rust核心重构实施记录.md`
10. **原生工程输出目录按配置隔离（2026-04-01）**：
   - `Payload` 与 `Injector` 的 `OutDir / IntDir` 已改为按 `$(Configuration)` 隔离。
   - 解决了 Debug/Release 共用中间产物导致的 `.obj/.pdb` 串配置与并行构建不稳定问题。
   - 保留 `artifacts/run` 作为统一运行目录，不影响现有运行习惯。
   - 详细记录见：`docs/Rust核心重构实施记录.md`
11. **MasterGUI 新增一键导出诊断日志（2026-04-02）**：
   - 主窗口新增“导出诊断日志”按钮。
   - 现在可以按当前会话自动收集 `master / injector / sync / payload / helper` 等日志、关键配置与 successfile，并合并为单个文本文件。
   - 统一导出目录：`artifacts/run/logs/exports/`
   - 用于真实环境复现问题后一次性给出可分析的诊断日志。
12. **Sync 输入架构重构基线与总方案（2026-04-02）**：
   - 已将当前“可用但仍有小概率方向持续移动”的版本冻结为基线分支：`baseline/sync-usable-20260402`，基线提交：`5cbdc08`。
   - 新增执行主文档：`docs/Sync输入架构重构落地方案.md`。
   - 该文档明确废弃 `neutral raw / vkey=0x00` 方向键占位策略，后续改为“单一逻辑状态 + 分层输入适配器 + 路径观测驱动”的重构路线。
   - 用于指导后续 Sync 输入执行层重构，并作为阶段实施、回退与验收依据。
13. **Rust 后续全面重构计划（2026-04-02）**：
   - 新增：`docs/Rust后续全面重构计划.md`
   - 基于当前仓库真实代码现状，对 `Injector`、`Payload/Sync`、控制端采集内核、`HelperMod` 与协议主源做了逐模块 Rust 化评估。
   - 明确后续优先级：先继续收口 `Injector` 与 `Sync`，再新增 `game_control_core`，最后推进 `Helper` 核心与协议生成/校验体系。
   - 同时明确不建议把 WPF UI、`DllMain`、MinHook/trampoline、游戏私有 ABI patch 层作为 Rust 重构主战场。
14. **重构文档目录统一（2026-04-07）**：
   - 新增目录：`docs/rust重构/`
   - 新增总览文档：`docs/rust重构/Rust重构现状、最终目标与构建约束总览.md`
   - 从这一步开始，后续所有重构相关文档统一放入 `docs/rust重构/` 目录，不再散落到 `docs/` 根目录。

---

## 第一阶段：目录结构重组 (手动操作)

请在磁盘上创建一个名为 `game-all` 的文件夹，并严格按以下结构组织文件。

> **⚠️ 注意** ：标有 `[搬运]` 的文件需要您从原项目中复制过来。

**Plaintext**

```
game-all/
├── 📁 Shared/
│   └── 📁 Protocols/                 <-- [新建] 协议单一来源（C++/C# 对齐）
├── 📁 Payload/                       <-- [C++ DLL 项目] 输出: game-payload.dll
│   ├── 📁 lib/                       <-- 第三方库目录
│   │   └── 📁 MinHook/               <-- MinHook 库 (必须放这里)
│   │       ├── MinHook.h             <-- [搬运]
│   │       └── MinHook.x86.lib       <-- [搬运]
│   ├── 📁 modules/
│   │   ├── 📁 helper/                <-- [搬运] 原 version-inject 目录下的 .h/.cpp
│   │   │   └── HelperMod.cpp         <-- [重命名] 由 version_proxy.cpp 改名而来
│   │   └── 📁 sync/                  <-- [搬运] 原 dnfinput 目录下的 .h/.cpp
│   │       └── SyncMod.cpp           <-- [重命名] 由 dnfinput.cpp 改名而来
│   ├── dllmain.cpp                   <-- [新建] 统一入口 (代码见下文)
│   ├── pch.h                         <-- [新建] 预编译头 (代码见下文)
│   └── pch.cpp                       <-- [新建] 预编译源文件
│
├── 📁 Injector/                      <-- [C++ Console 项目] 输出: game-injector.exe
│   └── main.cpp                      <-- [新建] APC 注入器源码 (代码见下文)
│
└── 📁 GUI/                           <-- [C# WPF 项目] 输出: game-master.exe
    ├── App.xaml
    ├── MainWindow.xaml               <-- [修改] 包含 TabControl 的主界面
    ├── 📁 Modules/
    │   ├── 📁 Helper/                <-- [搬运] 原 GameHelperGUI 代码
    │   └── 📁 Sync/                  <-- [搬运] 原 DNFSyncBox 代码
    └── ...
```

---

## 协议单一来源（Shared/Protocols）

**目标**：统一 C++ 与 C# 的共享内存结构定义与版本号，降低结构体错位风险。

**做法**：

1. 在 `Shared/Protocols/` 中维护协议定义文档（建议按模块拆分）：
   - `HelperStatusV5`、`HelperControlV4`
   - `SharedKeyboardStateV2`
   - **说明**：`HelperControlV4` 在 V3 基础上新增倍攻/怪物零伤动作字段，用于 UI/热键共享状态与一次性切换命令。
2. **强制版本与大小校验**：
   - GUI 端：读取/写入时校验 `Version` 与 `Size`。
   - Payload 端：写入/读取共享内存时，同样进行 `Version` 与 `Size` 校验。
3. 任何协议变更必须先更新 `Shared/Protocols/`，再改代码。
   - **Sync 协议回退为 V2（快照驱动）**：映射名称 `Local\\DNFSyncBox.KeyboardState.V2`。

**示例**（通俗说明）：

> “结构体定义就像一张表格，C++ 和 C# 都必须用同一份模板，否则字段会错位。”

---

## 协议引用（必读）

**唯一权威**：`Shared/Protocols/协议说明.md`

**要求**：

- 协议字段、版本号、大小校验必须以该文件为准。
- 任何代码变更前，先更新协议说明，再同步 C++ 与 C# 实现。
- 代码审查时以协议说明为对照，确保字段顺序与类型一致。

---

## 第二阶段：Payload (DLL) 代码改造

### 1. 配置预编译头 (`Payload/pch.h`)

在 VS 中设置项目使用预编译头，内容如下：

**C++**

```
#pragma once
#define WIN32_LEAN_AND_MEAN
#include <windows.h>
#include <vector>
#include <string>
#include <iostream>

// 引入 MinHook (路径已在第一阶段规划好)
#include "../lib/MinHook/MinHook.h"
```

### 2. 改造 Helper 模块 (`HelperMod.cpp`)

 **操作对象** ：`Payload/modules/helper/HelperMod.cpp` (原 `version_proxy.cpp`)

* **修改点 1** ：删除 `#include "version_exports.h"` 及所有 `#pragma comment(linker, ...)`。
* **修改点 2** ：在文件首尾添加 `namespace Helper { ... }`。
* **修改点 3** ：删除原有的 `DllMain`，改为 `Start` 函数。

**C++**

```
// 在 namespace Helper { 内部的末尾 ...

    // [新增] 启动接口
    void Start() {
        // 模拟原 DllMain 的初始化逻辑
        // 注意：g_self_module 变量需要在本文件顶部声明过
        g_self_module = GetModuleHandleA(NULL); 
      
        // 启动工作线程 (保留原逻辑)
        HANDLE thread = CreateThread(NULL, 0, WorkerThread, NULL, 0, NULL);
        if (thread) CloseHandle(thread);
    }

    void Stop() {
        // 清理逻辑
    }
} // namespace Helper 结束
```

### 3. 改造 Sync 模块 (`SyncMod.cpp`)

 **操作对象** ：`Payload/modules/sync/SyncMod.cpp` (原 `dnfinput.cpp`)

* 同样包裹 `namespace Sync { ... }`。
* 删除 `DllMain`，将初始化逻辑封装进 `Sync::Start()`。

### 4. 编写统一入口 (`Payload/dllmain.cpp`)

**C++**

```
#include "pch.h"
#include <atomic>

// 声明子模块接口
namespace Helper { void Start(); void Stop(); }
namespace Sync   { void Start(); void Stop(); }

std::atomic<bool> g_IsInitialized(false);

DWORD WINAPI UnifiedInitThread(LPVOID lpParam) {
    // 1. 初始化 MinHook (只需一次)
    if (MH_Initialize() != MH_OK) {
        OutputDebugStringA("[game-all] MinHook init failed!");
    }

    // 2. 启动同步模块 (建议先启动 Input Hook)
    Sync::Start();

    // 3. 启动辅助模块
    Helper::Start();

    return 0;
}

BOOL APIENTRY DllMain(HMODULE hModule, DWORD ul_reason_for_call, LPVOID lpReserved) {
    switch (ul_reason_for_call) {
    case DLL_PROCESS_ATTACH:
        // 原子锁防止 APC 重复触发
        bool expected = false;
        if (g_IsInitialized.compare_exchange_strong(expected, true)) {
            DisableThreadLibraryCalls(hModule);
            // 开启新线程进行初始化，避免 Loader Lock 死锁
            CreateThread(NULL, 0, UnifiedInitThread, NULL, 0, NULL);
        }
        break;
    }
    return TRUE;
}
```

---

## 第三阶段：Injector (注入器) 代码实现

 **操作对象** ：`Injector/main.cpp`

这是基于 `APCinjecter.cpp` 优化后的版本，增加了提权和等待功能。

**C++**

```
#include <windows.h>
#include <tlhelp32.h>
#include <vector>
#include <string>
#include <iostream>
#include <thread>
#include <chrono>

// 日志辅助
void Log(const std::wstring& msg) { std::wcout << L"[Injector] " << msg << std::endl; }

// 1. 提权 (SeDebugPrivilege)
bool EnableDebugPrivilege() {
    HANDLE hToken;
    TOKEN_PRIVILEGES tp;
    LUID luid;
    if (!OpenProcessToken(GetCurrentProcess(), TOKEN_ADJUST_PRIVILEGES | TOKEN_QUERY, &hToken)) return false;
    if (!LookupPrivilegeValue(NULL, SE_DEBUG_NAME, &luid)) { CloseHandle(hToken); return false; }
    tp.PrivilegeCount = 1;
    tp.Privileges[0].Luid = luid;
    tp.Privileges[0].Attributes = SE_PRIVILEGE_ENABLED;
    if (!AdjustTokenPrivileges(hToken, FALSE, &tp, sizeof(TOKEN_PRIVILEGES), NULL, NULL)) { CloseHandle(hToken); return false; }
    CloseHandle(hToken);
    return (GetLastError() == ERROR_SUCCESS);
}

// 2. 查找 PID
DWORD FindProcessId(const std::wstring& processName) {
    PROCESSENTRY32W pe32 = { sizeof(PROCESSENTRY32W) };
    HANDLE hSnapshot = CreateToolhelp32Snapshot(TH32CS_SNAPPROCESS, 0);
    if (hSnapshot == INVALID_HANDLE_VALUE) return 0;
    DWORD pid = 0;
    if (Process32FirstW(hSnapshot, &pe32)) {
        do {
            if (processName == pe32.szExeFile) { pid = pe32.th32ProcessID; break; }
        } while (Process32NextW(hSnapshot, &pe32));
    }
    CloseHandle(hSnapshot);
    return pid;
}

// 3. APC 注入
bool PerformApcInjection(DWORD pid, const std::wstring& dllPath) {
    HANDLE hProcess = OpenProcess(PROCESS_ALL_ACCESS, FALSE, pid);
    if (!hProcess) return false;

    size_t size = (dllPath.length() + 1) * sizeof(wchar_t);
    LPVOID remoteMem = VirtualAllocEx(hProcess, NULL, size, MEM_COMMIT, PAGE_READWRITE);
    if (!remoteMem) { CloseHandle(hProcess); return false; }

    if (!WriteProcessMemory(hProcess, remoteMem, dllPath.c_str(), size, NULL)) {
        VirtualFreeEx(hProcess, remoteMem, 0, MEM_RELEASE);
        CloseHandle(hProcess);
        return false;
    }

    // ⚠️ 关键：必须编译为 x86，否则 kernel32 地址不匹配
    PAPCFUNC pLoadLibrary = (PAPCFUNC)GetProcAddress(GetModuleHandleW(L"kernel32.dll"), "LoadLibraryW");
  
    HANDLE hSnapshot = CreateToolhelp32Snapshot(TH32CS_SNAPTHREAD, 0);
    THREADENTRY32 te32 = { sizeof(THREADENTRY32) };
    int queued = 0;

    if (Thread32First(hSnapshot, &te32)) {
        do {
            if (te32.th32OwnerProcessID == pid) {
                HANDLE hThread = OpenThread(THREAD_SET_CONTEXT, FALSE, te32.th32ThreadID);
                if (hThread) {
                    if (QueueUserAPC(pLoadLibrary, hThread, (ULONG_PTR)remoteMem)) queued++;
                    CloseHandle(hThread);
                }
            }
        } while (Thread32Next(hSnapshot, &te32));
    }
    CloseHandle(hSnapshot);
    CloseHandle(hProcess);
    return queued > 0;
}

int main() {
    SetConsoleTitleW(L"game-all Injector");
    if (!EnableDebugPrivilege()) Log(L"Warning: Failed to enable Debug Privilege.");

    wchar_t path[MAX_PATH];
    GetCurrentDirectoryW(MAX_PATH, path);
    std::wstring dllPath = std::wstring(path) + L"\\game-payload.dll"; // 目标 DLL 名称

    Log(L"Waiting for DNF.exe...");
    DWORD pid = 0;
    while ((pid = FindProcessId(L"DNF.exe")) == 0) {
        std::this_thread::sleep_for(std::chrono::milliseconds(1000));
    }

    Log(L"Found PID: " + std::to_wstring(pid));
    std::this_thread::sleep_for(std::chrono::milliseconds(2000)); // 等待初始化

    if (PerformApcInjection(pid, dllPath)) Log(L"Injection Success!");
    else Log(L"Injection Failed.");

    std::this_thread::sleep_for(std::chrono::seconds(3));
    return 0;
}
```

---

### 注入结果判定（当前实施）

为提高“模块隐藏”场景的稳定性，注入结果采用 **成功文件为主、共享内存心跳兜底**：

1. **成功文件（主判定）**  
   - 文件名：`successfile_%dll.name%_%pid%.txt`  
   - 默认目录：**DLL 所在目录下的 `logs` 子目录**（可通过 `injector.ini` 的 `output_dir` 覆盖）
   - 判定方式：记录注入前基线时间，注入后等待文件**新建或更新时间变化**。

2. **共享内存心跳（兜底）**  
   - 读取 `GameHelperStatus_{pid}`（Global/Local）  
   - 校验 `Version=3` 且 `Size=136`  
   - `LastTickMs` 距当前时间不超过阈值则判定成功

---

### Injector 配置（injector.ini）

注入器运行时读取同目录下 `injector.ini`，主要配置项如下：  
**模板文件**：`Injector/injector.ini`（构建后请复制到注入器输出目录）。

> 说明：当 `watch_mode=true` 时，注入器会**常驻监听**目标进程列表；  
> 若在 `idle_exit_seconds` 时间内**没有出现新的目标进程**，注入器将自动退出。

### 日志输出规范（统一 ./logs）

- **会话标识**：`./logs/session.current`（每次启动 MasterGUI 生成一次）
- **会话归档**：`./logs/session_<sessionId>/`（退出时复制对应会话日志）
- **MasterGUI（Debug）**：`./logs/master/master_<sessionId>_<pid>.log`
- **Injector（Debug）**：`./logs/injector/injector_<sessionId>_<pid>.log`
- **Payload（Debug）**：`./logs/payload/payload_<sessionId>_<pid>.log`
- **Helper（Payload）**：`./logs/helper/helper_<sessionId>_<pid>.log.jsonl`
- **Sync（GUI）**：`./logs/sync/gui/sync_gui_<sessionId>_<pid>.log`
- **Sync（Payload）**：`./logs/sync/payload/sync_payload_<sessionId>_<pid>.log`
- **成功文件**：默认 `DLL\\logs\\successfile_<dll>_<pid>.txt`

| 键 | 说明 | 默认值 |
| --- | --- | --- |
| process_name | 目标进程名（自动补 `.exe`） | `DNF.exe` |
| dll_path | 注入 DLL 路径（可相对） | `..\\payload\\game-payload.dll` |
| output_dir | 成功文件目录（空则使用 DLL\\logs 目录） | 空 |
| scan_interval_ms | 扫描进程间隔 | `1000` |
| inject_delay_ms | 发现进程后的注入延迟 | `2000` |
| max_retries | 重试次数 | `3` |
| retry_interval_ms | 重试间隔 | `1000` |
| success_timeout_ms | 成功文件判定超时 | `6000` |
| success_interval_ms | 成功文件轮询间隔 | `200` |
| heartbeat_timeout_ms | 心跳兜底超时 | `6000` |
| heartbeat_interval_ms | 心跳轮询间隔 | `200` |
| watch_mode | 是否常驻监听新进程 | `true` |
| idle_exit_seconds | 无新目标进程出现后自动退出（秒，0 不退出） | `600` |

---

## 第四阶段：Visual Studio 关键配置检查

为保证编译通过且运行正常，请对 `Payload` 和 `Injector` 项目执行以下检查：

| **项目**     | **属性页路径** | **设置项** | **值**                   | **说明**         |
| ------------------ | -------------------- | ---------------- | ------------------------------ | ---------------------- |
| **Payload**  | C/C++ -> 常规        | 附加包含目录     | `$(ProjectDir)lib\MinHook`   | 让代码能找到 MinHook.h |
| **Payload**  | 链接器 -> 常规       | 附加库目录       | `$(ProjectDir)lib\MinHook`   | 让链接器能找到 .lib    |
| **Payload**  | 链接器 -> 输入       | 附加依赖项       | `MinHook.x86.lib`            | 链接 MinHook           |
| **Both**     | 顶栏工具条           | 解决方案平台     | **x86**                  | 必须是 32 位           |
| **Both**     | 属性 -> 常规         | 字符集           | **使用 Unicode 字符集**  | 支持中文路径           |
| **Both**     | C/C++ -> 代码生成    | 运行库           | **多线程 (/MT)**         | 静态链接，避免缺少 DLL |
| **Injector** | 链接器 -> 清单文件   | UAC 执行级别     | **requireAdministrator** | 自动请求管理员权限     |

---

## 构建流程（建议执行顺序）

1. **设置平台**：解决方案平台统一为 `x86`。
2. **构建 Payload**：生成 `game-payload.dll`。
3. **构建 Injector**：生成 `game-injector.exe`。
4. **构建 MasterGUI**：目标框架统一 `net8.0-windows`。
5. **输出检查**：确认三组件文件名与目录一致。

---

## 构建前置条件

1. **工具链**：Visual Studio 2022（v143）与 C++ 工作负载已安装。
2. **平台**：解决方案平台选择 `x86`。
3. **MinHook 依赖**（Payload）：
   - `Payload/lib/minhook/include/MinHook.h`
   - `Payload/lib/minhook/lib/libMinHook.x86.lib`
   - 说明：`Payload/Payload.vcxproj` 已配置 `include` 与 `lib` 搜索路径，需按上述目录放置文件。
4. **.NET 运行时**：目标框架 `net8.0-windows` 已安装。

---

## 如何生成 MinHook 库

1. 打开解决方案：`Payload/lib/minhook/build/VC17/MinHookVC17.sln`
2. 选择平台：`Win32`，配置：`Release`
3. 在解决方案资源管理器中选中 **libMinHook** 项目并执行构建
4. 产物路径（示例）：
   - `Payload/lib/minhook/build/VC17/lib/Release/libMinHook.x86.lib`
5. 确认头文件存在：
   - `Payload/lib/minhook/include/MinHook.h`

**提示**：

- `Payload/Payload.vcxproj` 已配置 `include` 与 `build` 路径回退，确保能找到头文件与库。
- 若库名不同，请同步更新 `Payload/Payload.vcxproj` 的 `AdditionalDependencies`。

---

## 常见编译错误排查

1. **MinHook 找不到头文件或库**
   - 现象：`MinHook.h` 或 `libMinHook.x86.lib` 找不到。
   - 处理：确认以下路径存在文件：
     - `Payload/lib/minhook/include/MinHook.h`
     - `Payload/lib/minhook/build/VC17/lib/Release/libMinHook.x86.lib`
2. **平台不匹配（x64 / Any CPU）**
   - 现象：链接失败或运行时崩溃。
   - 处理：解决方案平台选择 `x86`，并确保 `.NET` 项目已设置 `PlatformTarget=x86`。
3. **工具链缺失**
   - 现象：提示找不到 `v143` 或 Windows SDK。
   - 处理：安装 VS2022 C++ 工作负载与对应 Windows SDK。
4. **清单权限问题（Injector）**
   - 现象：注入器启动无权限或提示 UAC。
   - 处理：确认 `Injector/app.manifest` 已嵌入并设置 `requireAdministrator`。

---

## 顶层解决方案与输出目录规范

**解决方案文件**：`game-all.sln`

**平台要求**：

- 解决方案平台统一为 `x86`。
- .NET 项目平台保持 `Any CPU`，但 `PlatformTarget` 强制为 `x86`。
- C++ 项目平台为 `Win32`。

**输出目录**（统一收敛到 `artifacts/`）：

- `GUI` 相关输出：`artifacts/bin/`
- `Payload` 输出：`artifacts/bin/payload/`
- `Injector` 输出：`artifacts/bin/injector/`
- 中间产物：`artifacts/obj/`

**说明**：

- .NET 输出路径通过 `Directory.Build.props` 统一。
- C++ 输出路径在 `*.vcxproj` 中显式指定。

---

## 第五阶段：共享内存一致性检查

这是最后一步，确保 GUI 能控制 DLL。

1. 在 `Payload/modules/helper/HelperMod.cpp` 中搜索 `CreateFileMapping`，确认名称为：
   `"Global\\GameHelperStatus_"` (或者您定义的特定名称)
2. 在 `GUI` 项目的 C# 代码中，确保 `SharedMemoryReader` 使用**完全相同**的字符串前缀。

完成以上步骤后，点击 Visual Studio 的  **"生成解决方案"** ，您将在 Release 目录下得到 `game-payload.dll` 和 `game-injector.exe`，直接运行 Injector 即可开始使用。

---

## MasterGUI 半集成说明

**定位**：MasterGUI 负责“控制与展示”，不直接接管注入器逻辑。

**具体行为**：

1. 提供“快捷入口”按钮（例如：打开注入器目录或提示运行 Injector）。
2. 通过共享内存检测状态：
   - Helper：读取 `GameHelperStatus_{pid}` 心跳或状态字段。
   - Sync：读取 `DNFSyncBox.KeyboardState.V2` 心跳或序列号变化。
3. 状态栏展示：
   - 未注入 / 运行中 / 超时失联 等状态提示。

**优势**：

- 权限与安全风险更低。
- 工程耦合更小，便于后期维护。
- 用户体验改善但不牺牲稳定性。

---

## GUI 集成细则（工程落地）

1. **主窗口结构**：
   - 使用 `TabControl` 承载两个模块页：Helper / Sync。
   - 底部统一状态栏展示注入状态与心跳结果。
2. **状态展示来源**：
   - Helper：读取 `GameHelperStatus_{pid}` 心跳或状态字段。
   - Sync：读取 `DNFSyncBox.KeyboardState.V2` 的 `LastTick/Seq`。
3. **快捷入口**：
   - 提供“打开注入器目录”或“提示运行 Injector”的按钮。
   - 不直接触发注入逻辑。
4. **异常提示**：
   - 共享内存不存在：提示“未注入或未启动”。
   - 心跳超时：提示“失联/异常”。

---

## MasterGUI 配置项说明

配置文件路径：`GUI/config/mastergui.json`

| 配置项 | 含义 | 默认值 |
| --- | --- | --- |
| TargetProcessNames | 目标进程名列表（不含 .exe），例如 `DNF`/`dnf` | `["DNF","dnf"]` |
| HelperHeartbeatTimeoutMs | Helper 心跳超时阈值（毫秒） | `2000` |
| SyncHeartbeatTimeoutMs | Sync 心跳超时阈值（毫秒） | `2000` |
| StatusIntervalMs | 状态刷新间隔（毫秒） | `1000` |
| SyncMappingName | Sync 共享内存名称 | `Local\\DNFSyncBox.KeyboardState.V2` |
| SyncVersion | Sync 协议版本 | `3` |
| SyncMappingSize | Sync 映射大小（字节） | `1824` |
| InjectorProcessNames | 注入器进程名列表 | `["game-injector","Injector"]` |
| RequireBothModulesForInjected | 是否要求 Helper 与 Sync 同时正常才显示“已注入” | `false` |

## 验收标准（完成定义）

1. **构建一致**：三组件在 `x86` 下成功编译。
2. **框架一致**：所有 GUI 项目目标框架统一为 `net8.0-windows`。
3. **协议一致**：C++/C# 与 `Shared/Protocols/协议说明.md` 对齐，版本/大小校验生效。
4. **GUI 可用**：主窗口正常加载两个模块页，状态栏有实时状态更新。
5. **半集成生效**：GUI 提供快捷入口且不会直接操控注入逻辑。

---

## 近期改动摘要（落地）

1. **Injector 并发注入任务化**
   - 发现新进程后独立线程等待窗口 → 固定等待 10s → 注入
   - 避免单进程阻塞全局扫描
2. **切窗 Grace + 高频前台检测**
   - 切换窗口后短时间内保持有效前台 PID，减少输入空窗
3. **共享心跳超时可配置**
   - 默认提升至 500ms，并支持环境变量覆盖
4. **连发键边沿合成**
   - 支持 RepeatKeys/RepeatIntervalMs，默认 X 连发
5. **性能优化**
   - 共享快照短缓存（默认 2ms）
   - RawInput 日志可开关与采样
6. **Payload 模块总开关**
   - 支持 payload.ini 控制 Helper/Sync 是否启用
   - 关闭模块仍保留 successfile
7. **Helper 线程与 IO 优化**
   - 低频任务合并为后台线程（共享内存写入/控制读取/配置重载/全屏攻击守护）
   - 吸怪与全屏技能逻辑合并调度，减少对象遍历
   - 输入轮询节流与按需读取（非前台/无功能时降低频率）
   - 配置热重载事件化（目录变更通知驱动，失败回退时间戳检测）
   - 日志写入异步化（队列缓冲+后台批量落盘）
8. **Sync 高并发输入路径优化**
   - 轻量快照判定 + 快路径分层，减少高频 Hook 的完整拷贝
   - 共享快照 seq 复用，稳定场景降低 memcpy
   - RawInput 仅在必要场景介入，减少切窗卡顿
   - 消息钩子按需短路（仅处理 WM_INPUT + RIM_INPUTSINK）
   - 统计计数开关化与节流，避免长期常驻开销
   - 暂停态兜底：即便 activePid=0 仍执行阻断逻辑，避免后台误响应
9. **暂停失效修复**
   - 快路径放宽对 activePid 的限制，暂停态仍拦截输入
10. **日志规范升级**
    - 会话标识 `session.current`，按会话归档到 `session_<id>/`
    - 模块日志分目录（master/injector/payload/helper/sync），文件名含 sessionId + pid

---

## 配置模板更新

1. `config-templates/injector.ini`
   - 新增：`window_wait_timeout_ms`、`window_poll_interval_ms`、`post_window_delay_ms`、`max_concurrent_tasks`
   - `inject_delay_ms` 默认改为 0（避免叠加等待）
2. `config-templates/profiles.json`
   - 新增连发键字段：`RepeatKeys`、`RepeatIntervalMs`
3. `config-templates/payload.ini`
   - 新增模块总开关：`EnableHelper`、`EnableSync`

---

## 运行参数（环境变量）

Payload 相关：
- `DNFSYNC_SPOOF_DELAY_MS`：注入后伪造延迟（毫秒）
- `DNFSYNC_HEARTBEAT_TIMEOUT_MS`：共享心跳超时（毫秒）
- `DNFSYNC_SNAPSHOT_CACHE_MS`：共享快照缓存时间（毫秒）
- `DNFSYNC_RAWINPUT_LOG`：RawInput 日志开关（1/true/yes/on）
- `DNFSYNC_RAWINPUT_LOG_INTERVAL_MS`：RawInput 日志采样间隔（毫秒）
- `config/sync_debug.ini`：同步模块调试配置（优先于环境变量）
- `sync_debug.ini` 关键字段：`Stats`/`StatsIntervalMs`、`Diag`/`DiagIntervalMs`、`RawInputLog`/`RawInputLogIntervalMs`、`KeyLog`/`KeyLogIntervalMs`/`KeyLogLevel`/`KeyUpTimeoutMs`
- `DNFSYNC_FORCE_DI_OK`：DirectInput 返回 DIERR_NOTACQUIRED 时强制 DI_OK
- `DNFSYNC_STATS`：Sync 统计日志开关（1/true/yes/on）
- `DNFSYNC_STATS_INTERVAL_MS`：统计日志输出间隔（毫秒）
- `DNFSYNC_DIAG`：Sync 诊断日志开关（1/true/yes/on）
- `DNFSYNC_DIAG_INTERVAL_MS`：诊断日志输出间隔（毫秒）
- `DNFSYNC_KEYLOG`：按键事件日志开关（1/true/yes/on）
- `DNFSYNC_KEYLOG_INTERVAL_MS`：按键事件日志采样间隔（毫秒）
- `DNFSYNC_KEYLOG_LEVEL`：按键日志级别（1=RawInput，2=含 Win32，3=含 DirectInput）
- `DNFSYNC_KEYUP_TIMEOUT_MS`：KeyUp 缺失告警阈值（毫秒）

Sync 配置文件：
- `profiles.json` 默认位置：`%AppData%\\DNFSyncBox\\profiles.json`
- 可参考模板：`config-templates/profiles.json`

---

## Rust 重构落地记录（2026-04-01）

### 7. Rust 完整重构计划落地（2026-04-01）
- 新增：`docs/Rust完整重构落地计划.md`
- 作用：把 Rust 重构目标、阶段划分、兼容策略、回退策略、测试方案一次性固化为执行基线。

### 8. Injector 接入 Rust 契约与默认配置（2026-04-01）
- `Injector` 已接入 `game_injector_core` 静态库。
- 默认 `injector.ini` 模板、数值默认值、`HelperStatusV5` 协议契约改由 Rust 导出。
- Windows x86 下已完成 `Injector.vcxproj` Debug 构建验证。

### 9. Payload 接入 Rust RuntimeDecision 与方向键缓存收敛（2026-04-01）
- `Payload` 已接入 `game_payload_core` 静态库。
- `SyncMod` 的 `Alive / Paused / Clear / Bypass` 运行时判断开始统一走 Rust `RuntimeDecision`。
- 共享快照缓存新增 Rust 方向键释放收敛仲裁，降低方向键松手后旧快照继续复用的概率。
- Windows 下已完成：
  - `game_payload_core` x86 Rust 构建
  - `Payload.vcxproj` Debug|Win32 构建
  - 顶层 `game-all.sln` Debug|x86 构建
  - 顶层 `game-all.sln` Release|x86 构建

### 10. 方向键强制抬起掩码接入执行端输入链（2026-04-01）
- `SyncMod` 线程缓存已开始保存 Rust `force release mask`。
- 强制抬起掩码已接入：
  - `GetAsyncKeyState`
  - `GetKeyboardState`
  - `GetRawInputBuffer`
  - `GetRawInputData`
  - `GetDeviceState`
- 顶层解决方案在本轮改动后再次完成 Debug|x86 / Release|x86 构建验证。

### 11. 单键决策收口到 Rust（2026-04-01）
- `game_payload_core` 新增 `KeyDecision` 与 `payload_core_evaluate_key_state_header(...)`。
- `SyncMod` 的 Win32 / RawInput / DirectInput 单键决策开始优先由 Rust 输出：
  - 目标键是否按下
  - 黑名单键是否应拦截
  - 方向键释放窗口是否应强制抬起
- 顶层解决方案在本轮继续保持 Debug|x86 / Release|x86 构建通过。

### 12. Sync 高频方向键残留定点修复（2026-04-02）
- 基于实机诊断日志确认：当前主问题集中在从端 `RawInputData` 链路，高频左右切换时会出现 `down` 残留、`up` 延迟和 `orphan_up/missing_keyup`。
- `SyncMod` 已新增方向键组裁决与 RawInput 过渡事件优先级：
  - 左右互斥、上下互斥
  - 先释放旧方向，再允许新方向按下
  - 在 `RawInputBuffer/GetRawInputData` 路径里优先处理方向键过渡，降低后台持续移动概率
- 新增 `[DIR]` 方向键闭环日志，便于后续直接判断 `snapshot/raw/win32` 是否一致。

### 13. 诊断导出改为共享读活跃日志（2026-04-02）
- `DiagnosticExportService` 读取活跃日志时改为 `FileShare.ReadWrite | FileShare.Delete`。
- 导出单文件时不再因为 payload/helper 日志仍被进程占用而大面积“读取失败”。

### 14. Sync 输入架构重构主线落地（2026-04-02）
- 按 `docs/Sync输入架构重构落地方案.md` 继续推进 Sync 执行层重构，先把“方向键/RawInput 重复流”从补丁修复转为逻辑态驱动。
- `game_payload_core` 新增：
  - `LogicalKeyDecision`
  - `ChannelEmitDecision`
  - `payload_core_evaluate_logical_key_header(...)`
  - `payload_core_decide_channel_emit(...)`
- 方向键对冲、边沿判断、通道发射决策开始由 Rust 输出，C++ 主要保留 Hook/RawInput/DirectInput 边界适配。

### 15. 移除 neutral raw 语义并改为边沿投影（2026-04-02）
- `SyncMod` 中 Mapping RawInput 输出已改为“只在状态变化时输出真实 transition”。
- 不再复用“长按期间继续输出重复 down / neutral 占位事件”的旧语义。
- 新增 `raw_neutral_suppressed` 诊断日志，用于确认旧的 neutral raw 路径已被抑制。

### 16. RawInput 消息提升与投影统一收口（2026-04-02）
- `FixWmInputMessage(...)` 不再无条件把后台 `WM_INPUT/RIM_INPUTSINK` 提升为前台 `RIM_INPUT`。
- 现在只有在存在真实逻辑 transition 时才提升 RawInput 消息，避免把系统自动重复的 key-down 流直接重放到从端。
- `GetRawInputData/GetRawInputBuffer` 改为走统一 logical projection：
  - 方向键先释放旧方向，再按下新方向
  - 通道按 `projected_down_before -> projected_down_after` 投影
  - 重复 down 被识别为 `suppress_repeat`

### 17. 新增高信号 Sync 诊断日志（2026-04-02）
- 新增日志标签：
  - `[EDGE]`：逻辑边沿
  - `[EMIT]`：适配器实际投影
  - `[GROUP]`：方向互斥组裁决
  - `[REPEAT]`：重复事件被抑制
  - `[OBS]`：当前输入路径观测摘要
- 后续复盘方向键持续移动问题时，优先看以上日志，不再只依赖 `[KEYWARN]`。

### 18. 诊断导出器补强（2026-04-02）
- `DiagnosticExportService` 已补强：
  - `sync_payload_<session>_<pid>.log`
  - `sync_gui_<session>_<pid>.log`
  会被优先纳入当前 session 的单文件导出。
- 导出器读取活跃日志时新增“共享读失败 -> 复制临时文件再读”的回退路径。
- `Sync 关键摘录` 已纳入 `[EDGE]/[EMIT]/[GROUP]/[REPEAT]/[OBS]`。

### 19. 本轮验证记录（2026-04-02）
- 已完成本地 Rust 验证：
  - `cargo test -p game_payload_core`
  - `cargo clippy -p game_payload_core --all-targets --all-features -- -D warnings`
- 已完成 Windows 侧可执行编译验证（当前机器仅有 VS2019 BuildTools，可用 `v142` 覆盖测试）：
  - `Payload.vcxproj /t:ClCompile /p:Configuration=Debug /p:Platform=Win32 /p:PlatformToolset=v142`
  - `Payload.vcxproj /t:ClCompile /p:Configuration=Release /p:Platform=Win32 /p:PlatformToolset=v142`
- 当前 Windows 环境无法完成顶层原生链接/整包构建，原因：
  - 本机缺少项目默认要求的 `v145` 工具链；
  - 使用现有 VS2019 BuildTools (`v142`) 时，链接阶段出现工具链版本不匹配（Rust/MinHook/Linker P1/P2 mismatch）。
- 因此本轮已完成“Rust 测试 + Payload C++ 编译级验证 + 导出器代码落地”，整包链接仍需在具备 `v145/VS2022` 的 Windows 机器上复验。

### 20. 暂停语义修复与 RawInput 静默态收口（2026-04-02）
- 修复暂停回归：`Paused=1` / `Clear=1` / 心跳失活时，从端不再只是“停止 spoof”，而是进入统一静默输入态。
- `GetAsyncKeyState`、`GetKeyboardState`、`GetDeviceState` 在后台从端静默态下会统一返回释放视图，避免手动暂停和自动暂停失效。
- `GetRawInputData` / `GetRawInputBuffer` 新增静默态分支：
  - 优先释放当前仍残留的 `g_lastRawKeyboardState`
  - 若无残留，则把当前键盘 RawInput 中和为 `keyup`
  - 避免暂停期间后台继续消费真实方向键 `down` 流
- 新增 `[PAUSE]` 日志，并把诊断导出器关键摘录扩展到 `[PAUSE]` / `[STATE]`，后续复盘暂停失效时可直接定位是否仍有 pass-through 输入。

### 21. Windows v142 构建兼容性补强（2026-04-02）
- `Payload.vcxproj` 新增 MinHook 条件链接目录：
  - `PlatformToolset=v142` 时优先链接 `Payload/lib/minhook/build/VC16/lib/Release/libMinHook.x86.lib`
  - 默认 `v145` 仍走现有 `VC17` 产物路径
- 因此当前机器（VS2019 BuildTools / `v142`）下已完成：
  - `Injector.vcxproj` Debug|Win32 Build
  - `Injector.vcxproj` Release|Win32 Build
  - `Payload.vcxproj` Debug|Win32 Rebuild
  - `Payload.vcxproj` Release|Win32 Build
  - `DNFSyncBox.csproj` Debug / Release Build
- Rust workspace 已完成：
  - `cargo test`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- 当前仍无法在本机完成 `GameHelperGUI` / `GameMasterGUI` 的完整 Windows 构建：
  - `GameHelperGUI.csproj` 仍通过 `ProjectReference` 依赖 `Injector.vcxproj`
  - 现有 `dotnet SDK 10.0.104` 与 VS2019 C++ MSBuild 任务在混合项目图下存在兼容问题，缺少 VS2022 / MSBuild 17 C++ 构建链

### 22. GUI 与 Injector 构建图解耦（2026-04-02）
- `GameHelperGUI.csproj` 已移除对 `Injector/Injector.vcxproj` 的硬 `ProjectReference`。
- `GameMasterGUI.csproj` 已修正 `PostBuildCopyToRun` 的 `RepoRoot` 回退逻辑：
  - `dotnet build` 下若 `$(SolutionDir)` 为 `*Undefined*`，自动回退到仓库根目录推导
  - 不再因无效路径导致 `artifacts/run/` 创建失败
- 当前推荐构建方式改为“GUI 与原生项目分离构建”：
  - GUI：`dotnet build`
  - Injector / Payload：`MSBuild.exe ... /p:PlatformToolset=v142`
- 本机已完成验证：
  - `GameHelperGUI.csproj` Debug / Release Build
  - `GameMasterGUI.csproj` Debug / Release Build
  - `DNFSyncBox.csproj` Debug / Release Build
  - `Injector.vcxproj` Debug Build (`v142`)
  - `Payload.vcxproj` Debug Build (`v142`)
- 新边界与项目既有“半集成”设计一致：
  - GUI 只负责状态展示与打开注入器目录
  - Injector 继续独立原生构建与运行

### 23. Injector 运行计划与 watch_mode 状态机继续收口到 Rust（2026-04-07）
- `game_injector_core` 新增：
  - `InjectorRuntimePlan`
  - `InjectorWatchRuntime`
- `Injector/main.cpp` 已开始改为由 Rust 统一负责：
  - `injector.ini` 文本解析与路径归一化
  - watch_mode 下的任务状态维护
  - pending/running/finished/removal 生命周期决策
  - idle 自动退出判定
- 当前仍保留在 C++：
  - `QueueUserAPC` 注入动作
  - `WaitForProcessWindow(...)`
  - successfile / heartbeat 的具体验证循环
  - Win32 句柄与日志边界
- 本机已完成验证：
  - `cargo test --workspace`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  - `Injector.vcxproj` Debug|Win32 Build (`v142`)
  - `Injector.vcxproj` Release|Win32 Build (`v142`)

### 24. Injector 成功判定与重试策略继续收口到 Rust（2026-04-07）
- `game_injector_core` 新增：
  - `HelperHeartbeatDecision`
  - `InjectionRetryRuntime`
  - `InjectionRetryDecision`
- `Injector/main.cpp` 已开始改为由 Rust 统一负责：
  - `HelperStatusV5` 心跳有效性判断
  - successfile / heartbeat 汇总后的成功判定
  - 单次注入尝试是否继续重试
  - retry interval 与 finished 判定
- 当前仍保留在 C++：
  - `WaitForSuccessFile(...)` 文件等待循环
  - `HasHelperHeartbeat(...)` 的共享内存读取壳
  - `QueueUserAPC` 注入动作
  - `WaitForProcessWindow(...)`
- 本机已完成验证：
  - `cargo test --workspace`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  - `Injector.vcxproj` Debug|Win32 Build (`v142`)
  - `Injector.vcxproj` Release|Win32 Build (`v142`)

### 25. Injector 注入后端抽象与 successfile 观察升级（2026-04-07）
- `InjectorConfig` / `injector.ini` 已新增配置项：
  - `inject_backend=apc`
  - `success_observer_mode=notify`
- `Injector/main.cpp` 已新增：
  - `TryWaitForInputIdleHint(...)`
  - `ProbeProcessWindowReady(...)`
  - `ObserveSuccessFileChange(...)`
  - `PerformInjectionWithBackend(...)`
- 当前行为调整为：
  - `WaitForInputIdle` 仅作为窗口初始化提示信号
  - 仍由窗口枚举确认“窗口是否真正就绪”
  - successfile 默认使用目录变更通知，失败时回退轮询
  - 注入后端开始抽象，但当前仍默认 APC
- 本机已完成验证：
  - `cargo test --workspace`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  - `Injector.vcxproj` Debug|Win32 Build (`v142`)
  - `Injector.vcxproj` Release|Win32 Build (`v142`)

### 26. Injector 结构化平台观测结果与 fallback 自动降级（2026-04-07）
- `game_injector_core` / `game_injector_core_ffi.h` 已新增结构化结果类型：
  - `InjectorWindowProbeResultInterop`
  - `InjectorSuccessObservationResultInterop`
  - `InjectorHeartbeatObservationResultInterop`
  - `InjectorBackendExecutionResultInterop`
- `Injector/main.cpp` 的以下边界适配器已改为返回结构化结果：
  - `ProbeProcessWindowReady(...)`
  - `ObserveSuccessFileChange(...)`
  - `ObserveHelperHeartbeat(...)`
  - `PerformInjectionWithBackend(...)`
- 当前 `inject_backend=fallback` 的运行语义已固定：
  - 自动降级为 `apc`
  - 在日志和结构化结果里显式标记 `downgraded=1`
- `TryInjectProcess(...)` 已改为把 backend/success/heartbeat 结构化观测统一交由 Rust 推进 attempt 决策。
- 本机已完成验证：
  - `cargo test --workspace`
  - `cargo clippy -p game_injector_core --all-targets --all-features -- -D warnings`
  - `Injector.vcxproj` Debug|Win32 Build (`v142`)
  - `Injector.vcxproj` Release|Win32 Build (`v142`)

### 27. Sync 路径观测开始收口到 Rust（2026-04-07）
- `game_payload_core` 已新增：
  - `InputChannelKind`
  - `InputPathObservation`
  - `AdapterProjectedState`
- `game_payload_core_ffi` 已新增：
  - `payload_core_observe_input_path(...)`
  - `payload_core_evaluate_adapter_projected_state(...)`
- `SyncMod.cpp` 的 `[OBS]` 日志已开始改为由 Rust 统一输出：
  - `channel_kind`
  - `mixed_inputs`
  - `raw_active`
  - `direct_input_active`
  - `win32_active`
- 现有 RawInput > DirectInput > Win32 的通道优先级已在 Rust 测试中固化。
- 本机已完成验证：
  - `cargo test --workspace`
  - `cargo clippy -p game_payload_core --all-targets --all-features -- -D warnings`
  - `Payload.vcxproj` Debug|Win32 Build (`v142`)
  - `Payload.vcxproj` Release|Win32 Build (`v142`)
  - `GameMasterGUI.csproj` Debug / Release Build

### 28. Sync drift 汇总继续收口到 Rust（2026-04-07）
- `game_payload_core` 已新增：
  - `AdapterDriftSummary`
  - `payload_core_summarize_adapter_drift(...)`
- `SyncMod.cpp` 的 `[OBS]` 日志已继续改为由 Rust 输出：
  - `drift_raw`
  - `drift_win32`
  - `drift_di`
- 当前 `Sync` 观测链已经形成：
  - 路径通道摘要：Rust
  - mixed/raw/di/win32 活跃态：Rust
  - 适配器 drift 汇总：Rust
- 本机已完成验证：
  - `cargo test --workspace`
  - `cargo clippy -p game_payload_core --all-targets --all-features -- -D warnings`
  - `Payload.vcxproj` Debug|Win32 Build (`v142`)
  - `Payload.vcxproj` Release|Win32 Build (`v142`)

### 29. Sync state store 开始从 C++ 迁入 Rust（2026-04-07）
- `game_payload_core::sync` 已新增：
  - `ProjectedChannelKind`
  - `SyncStateStore`
- `game_payload_core_ffi` 已新增：
  - `payload_core_state_store_create(...)`
  - `payload_core_state_store_set_logical_desired(...)`
  - `payload_core_state_store_get_logical_desired(...)`
  - `payload_core_state_store_set_projected(...)`
  - `payload_core_state_store_get_projected(...)`
  - `payload_core_state_store_clear_logical_desired(...)`
  - `payload_core_state_store_clear_all_projected(...)`
- `SyncMod.cpp` 已开始改为：
  - Rust store 为主
  - C++ 四个状态数组为镜像缓存
- 当前已接入的关键路径包括：
  - clear
  - projected state 推进
  - logical desired 前态读取
  - Win32/DirectInput projected 记录
- 本机已完成验证：
  - `cargo test -p game_payload_core`
  - `cargo clippy -p game_payload_core --all-targets --all-features -- -D warnings`
  - `Payload.vcxproj` Debug|Win32 Build (`v142`)
  - `Payload.vcxproj` Release|Win32 Build (`v142`)

### 30. Sync pause release plan 开始收口到 Rust（2026-04-07）
- `game_payload_core::sync` 已新增：
  - `PauseReleaseReason`
  - `PauseReleaseDecision`
  - `SyncStateStore::pick_pause_release(...)`
- `SyncMod.cpp` 的 `TryPickSilentRawTransition(...)` 已开始改为：
  - Rust 决定 pause/静默态优先释放哪个键
  - C++ 只负责 RawInput 投影与 `[PAUSE]` 日志
- 当前 Rust 已接管的释放顺序语义：
  - preferred key
  - direction pair
  - any direction key
  - stale projected key
  - neutralize preferred key
- 本机已完成验证：
  - `cargo test -p game_payload_core`
  - `cargo clippy -p game_payload_core --all-targets --all-features -- -D warnings`
  - `Payload.vcxproj` Debug|Win32 Build (`v142`)

### 31. Sync clear reset 开始收口到 Rust（2026-04-07）
- `game_payload_core::sync` 已新增：
  - `ClearResetDecision`
  - `SyncStateStore::apply_clear_reset(...)`
- `SyncMod.cpp` 已开始改为：
  - `ApplyClearIfNeeded(...)` 先走 Rust clear reset 决策
  - `ApplyRawClearIfNeeded(...)` 先走 Rust projected clear 决策
- 当前 `clear` 语义已开始与 Rust state store 对齐，不再只靠 C++ 直接清零。
- 本机已完成验证：
  - `cargo test -p game_payload_core`
  - `cargo clippy -p game_payload_core --all-targets --all-features -- -D warnings`
  - `Payload.vcxproj` Debug|Win32 Build (`v142`)
  - `Payload.vcxproj` Release|Win32 Build (`v142`)

### 32. Sync observation snapshot 开始收口到 Rust（2026-04-07）
- `game_payload_core::diagnostics` 已新增：
  - `SyncObservationSnapshot`
  - `build_sync_observation_snapshot(...)`
- `game_payload_core_ffi` 已新增：
  - `PayloadSyncObservationSnapshotInterop`
  - `payload_core_build_sync_observation_snapshot(...)`
- `SyncMod.cpp` 当前已改为：
  - 路径观测：Rust
  - drift 汇总：Rust
  - observation snapshot：Rust
  - C++ 只负责把快照格式化输出到 `[OBS]`
- 本机已完成验证：
  - `cargo test -p game_payload_core`
  - `cargo clippy -p game_payload_core --all-targets --all-features -- -D warnings`
  - `Payload.vcxproj` Debug|Win32 Build (`v142`)
  - `Payload.vcxproj` Release|Win32 Build (`v142`)

### 33. Sync adapter diagnostics event/buffer 开始收口到 Rust（2026-04-07）
- `game_payload_core::diagnostics` 已新增：
  - `AdapterDiagnosticsEventKind`
  - `AdapterDiagnosticsEvent`
  - `AdapterDiagnosticsBuffer`
- `SyncMod.cpp` 已开始把以下事件推入 Rust diagnostics buffer：
  - `LogicalChanged`
  - `AdapterEmitted`
  - `PauseRelease`
  - `ClearApplied`
- 当前 `Sync` diagnostics 已形成：
  - snapshot：`SyncObservationSnapshot`
  - event：`AdapterDiagnosticsEvent`
  - buffer：`AdapterDiagnosticsBuffer`
- 本机已完成验证：
  - `cargo test -p game_payload_core`
  - `cargo clippy -p game_payload_core --all-targets --all-features -- -D warnings`
  - `Payload.vcxproj` Debug|Win32 Build (`v142`)
  - `Payload.vcxproj` Release|Win32 Build (`v142`)

### 34. Sync Rust diagnostics 已接入导出链（2026-04-07）
- `SyncMod.cpp` 已开始输出：
  - `[RUSTDIAG] snapshot ...`
  - `[RUSTDIAG] event ...`
- `DiagnosticExportService` 已把 `[RUSTDIAG]` 纳入 `SyncFocusKeywords`
- 当前导出链变为：
  - Rust diagnostics buffer / snapshot
  - Payload 日志落盘
  - GUI 单文件导出采集
- 本机已完成验证：
  - `cargo test -p game_payload_core`
  - `cargo clippy -p game_payload_core --all-targets --all-features -- -D warnings`
  - `Payload.vcxproj` Debug|Win32 Build (`v142`)
  - `Payload.vcxproj` Release|Win32 Build (`v142`)
  - `GameMasterGUI.csproj` Debug / Release Build

### 35. Sync projected state 更新入口继续收口到 Rust（2026-04-07）
- `game_payload_core::sync` 已新增：
  - `ProjectedStateUpdate`
  - `SyncStateStore::update_projected(...)`
- `game_payload_core_ffi` 已新增：
  - `PayloadProjectedStateUpdateInterop`
  - `payload_core_state_store_update_projected(...)`
- `SyncMod.cpp` 当前已改为：
  - `TryPickMappingRawTransition(...)` 通过 Rust store 推进 Raw projected
  - `TryPickDirectionTransition(...)` 通过 Rust store 推进 Raw projected
  - `RecordWin32KeyEventIfNeeded(...)` 无论日志是否开启都会推进 Win32 projected
  - `RecordDirectInputKeyEventIfNeeded(...)` 无论日志是否开启都会推进 DirectInput projected
  - `TryPickLogicalRawTransition(...)` 直接使用 Rust 返回的 `projected_down_before / projected_down_after`
- 本轮顺手修正了一个关键问题：
  - `decide_channel_emit_with_store(...)` 已经推进 projected state 后，C++ 不再回读 store 把 `before` 记错
- 本机已完成验证：
  - `cargo test -p game_payload_core`
  - `cargo clippy -p game_payload_core --all-targets --all-features -- -D warnings`
  - `Payload.vcxproj` Debug|Win32 Build (`v142`)
  - `Payload.vcxproj` Release|Win32 Build (`v142`)

### 36. Sync adapter transition reason 开始统一由 Rust 输出（2026-04-07）
- `game_payload_core::sync` 已新增：
  - `ChannelTransitionReason`
  - `ProjectedStateUpdate.transition_reason`
- `game_payload_core::runtime` 已新增：
  - `ChannelEmitDecision.transition_reason`
- `SyncMod.cpp` 当前已改为：
  - Raw `emit` 路径直接消费 Rust `transition_reason`
  - Win32 / DirectInput projected update 也会写入 Rust `transition_reason`
  - `[EMIT]` 日志会输出 `transition=...`
  - `AdapterDiagnosticsEvent.reason_code` 统一记录 Rust transition reason，而不是仅记录 `emit_action`
- 当前统一后的 reason 语义包括：
  - `desired_press`
  - `desired_release`
  - `blocked_release`
  - `repeat_suppressed`
  - `observed_press`
  - `observed_release`
- 本机已完成验证：
  - `cargo test -p game_payload_core`
  - `cargo clippy -p game_payload_core --all-targets --all-features -- -D warnings`
  - `Payload.vcxproj` Debug|Win32 Build (`v142`)
  - `Payload.vcxproj` Release|Win32 Build (`v142`)

### 37. Sync mapping / direction 选择策略开始统一由 Rust 输出（2026-04-07）
- `game_payload_core::sync` 已新增：
  - `MappingTransitionReason`
  - `DirectionSelectionReason`
  - `MappingTransitionDecision`
  - `DirectionTransitionDecision`
- `game_payload_core_ffi` 已新增：
  - `payload_core_state_store_select_mapping_transition(...)`
  - `payload_core_state_store_select_direction_transition(...)`
- `SyncMod.cpp` 当前已改为：
  - `TryPickMappingRawTransition(...)` 直接消费 Rust 决策
  - `TryPickDirectionTransition(...)` 直接消费 Rust 决策
  - C++ 不再自己扫描 `targetMask/keyboardState` 或手动构造方向键 desired state
- 本轮已收口到 Rust 的 selection reason 包括：
  - `mapping_edge_down`
  - `mapping_edge_up`
  - `raw_neutral_suppressed`
  - `preferred_release`
  - `force_release_mask`
  - `stale_raw_down`
  - `preferred_press`
  - `group_winner_press`
- 本机已完成验证：
  - `cargo test -p game_payload_core`
  - `cargo clippy -p game_payload_core --all-targets --all-features -- -D warnings`
  - `Payload.vcxproj` Debug|Win32 Build (`v142`)
  - `Payload.vcxproj` Release|Win32 Build (`v142`)

### 38. Sync logical raw 候选顺序与 selection reason 开始统一由 Rust 输出（2026-04-07）
- `game_payload_core::runtime` 已新增：
  - `LogicalRawSelectionReason`
  - `LogicalRawCandidate`
  - `LogicalRawPlan`
  - `build_logical_raw_plan(...)`
- `game_payload_core_ffi` 已新增：
  - `PayloadLogicalRawPlanInterop`
  - `payload_core_build_logical_raw_plan(...)`
- `SyncMod.cpp` 当前已改为：
  - `TryPickLogicalRawTransition(...)` 直接消费 Rust candidate plan
  - C++ 不再手写 `direction_release_first / logical_release / logical_press / group_winner_press / logical_emit` 的候选顺序
- 当前 logical raw 这条路径里，Rust 已经负责：
  - 候选顺序
  - `observed_down`
  - `required_action`
  - `selection_reason`
- 本机已完成验证：
  - `cargo test -p game_payload_core`
  - `cargo clippy -p game_payload_core --all-targets --all-features -- -D warnings`
  - `Payload.vcxproj` Debug|Win32 Build (`v142`)
  - `Payload.vcxproj` Release|Win32 Build (`v142`)

### 39. Sync logical raw emit 决策开始统一由 Rust 输出（2026-04-07）
- `game_payload_core::runtime` 已新增：
  - `LogicalRawTransitionDecision`
  - `decide_logical_raw_transition_with_store(...)`
- `game_payload_core_ffi` 已新增：
  - `PayloadLogicalRawTransitionInterop`
  - `payload_core_state_store_decide_logical_raw_transition(...)`
- `SyncMod.cpp` 当前已改为：
  - `TryPickLogicalRawTransition(...)` 直接消费 Rust 最终 decision
  - C++ 不再自己逐 candidate 调 `EvaluateChannelEmitDecision(...)` 后再筛选
- Rust 当前已经能统一给出：
  - 最终 emit 的 `vkey`
  - `emit_action`
  - `projected_before / projected_after`
  - `selection_reason`
  - `transition_reason`
  - `pressed_edge / released_edge`
  - `repeat_suppressed` 诊断
- 本机已完成验证：
  - `cargo test -p game_payload_core`
  - `cargo clippy -p game_payload_core --all-targets --all-features -- -D warnings`
  - `Payload.vcxproj` Debug|Win32 Build (`v142`)
  - `Payload.vcxproj` Release|Win32 Build (`v142`)

### 40. Sync direction group 诊断开始统一由 Rust 输出（2026-04-07）
- `game_payload_core::runtime` 已新增：
  - `DirectionGroupReason`
  - `DirectionGroupDecision`
  - `evaluate_direction_group_decision(...)`
- `game_payload_core_ffi` 已新增：
  - `PayloadDirectionGroupDecisionInterop`
  - `payload_core_evaluate_direction_group_decision(...)`
- `SyncMod.cpp` 当前已改为：
  - `EvaluateLogicalKeyDecision(...)` 直接消费 Rust 的 direction group 诊断
  - C++ 不再自己计算 winner / loser / `edge_counter` / `edge_tie_release`
- 本机已完成验证：
  - `cargo test -p game_payload_core`
  - `cargo clippy -p game_payload_core --all-targets --all-features -- -D warnings`
  - `Payload.vcxproj` Debug|Win32 Build (`v142`)
  - `Payload.vcxproj` Release|Win32 Build (`v142`)

### 41. Injector attempt 结果解释继续收口到 Rust（2026-04-08）
- `game_injector_core::runtime` 已新增：
  - `AttemptOutcomeCode`
  - `AttemptOutcomeSummary`
  - `summarize_attempt_outcome(...)`
- `game_injector_core_ffi` 已新增：
  - `InjectorAttemptOutcomeSummaryInterop`
  - `injector_core_summarize_attempt_outcome(...)`
- `Injector/main.cpp` 当前已改为：
  - `TryInjectProcess(...)` 直接消费 Rust 汇总后的 attempt outcome
  - C++ 不再自己解释 `successfile / heartbeat / mapping / contract` 的组合结果
- 当前已由 Rust 统一解释的结果包括：
  - `successfile` 成功
  - `heartbeat` 成功
  - backend 未启动
  - heartbeat 映射缺失
  - heartbeat 协议不匹配
  - successfile / heartbeat 均超时
- 本机已完成验证：
  - `cargo test -p game_injector_core`
  - `cargo clippy -p game_injector_core --all-targets --all-features -- -D warnings`
  - `Injector.vcxproj` Debug|Win32 Build (`v142`)
  - `Injector.vcxproj` Release|Win32 Build (`v142`)

### 42. Injector attempt 失败原因开始统一由 Rust 汇总（2026-04-08）
- `game_injector_core::runtime` 已新增：
  - `AttemptOutcomeCode`
  - `AttemptOutcomeSummary`
  - `summarize_attempt_outcome(...)`
- `game_injector_core_ffi` 已新增：
  - `InjectorAttemptOutcomeSummaryInterop`
  - `injector_core_summarize_attempt_outcome(...)`
- `Injector/main.cpp` 当前已改为：
  - `TryInjectProcess(...)` 直接消费 Rust 汇总后的 attempt outcome
  - C++ 不再自己解释 `mapping_found / contract_ok / timeout` 组合
- 当前已由 Rust 汇总的结果包括：
  - successfile 成功
  - heartbeat 成功
  - backend 未启动
  - heartbeat 映射缺失
  - heartbeat 协议不匹配
  - successfile / heartbeat 均超时
- 本机已完成验证：
  - `cargo test -p game_injector_core`
  - `cargo clippy -p game_injector_core --all-targets --all-features -- -D warnings`
  - `Injector.vcxproj` Debug|Win32 Build (`v142`)
  - `Injector.vcxproj` Release|Win32 Build (`v142`)

### 43. 新增 game_control_core 最小内核与 C ABI（2026-04-08）
- Rust workspace 已新增：
  - `crates/game_control_core`
- 当前已实现的控制端内核逻辑包括：
  - foreground grace 判定
  - disable auto pause 行为
  - `auto_paused` 计算
  - 共享快照头部 `flags / active_pid / profile_id / profile_mode / last_tick`
- 当前已新增 C ABI：
  - `game_control_core_evaluate_foreground(...)`
  - `game_control_core_build_publish_header(...)`
- 这一步还未接入 `SyncController.cs`，但已经完成：
  - Rust 逻辑实现
  - C ABI 边界
  - workspace 验证
- 本机已完成验证：
  - `cargo test -p game_control_core`
  - `cargo clippy -p game_control_core --all-targets --all-features -- -D warnings`
  - `cargo test --workspace`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`

### 44. 新增 game_helper_core 最小协议与控制计划内核（2026-04-08）
- Rust workspace 已新增：
  - `crates/game_helper_core`
- 当前已落地的 Helper 核心包括：
  - `HelperStatus` 协议契约校验
  - `HelperControl` 协议契约校验
  - `ActionMask` -> 控制应用计划解码
- 当前已覆盖的动作位包括：
  - fullscreen attack
  - fullscreen skill
  - auto transparent
  - hotkey enabled
  - attract enabled / mode / positive
  - gather items
  - damage enabled / multiplier
  - invincible enabled
- 这一步还未接回 `HelperMod.cpp`，但已经完成：
  - Rust 逻辑实现
  - crate 接入 workspace
  - workspace 全量验证
- 本机已完成验证：
  - `cargo test -p game_helper_core`
  - `cargo clippy -p game_helper_core --all-targets --all-features -- -D warnings`
  - `cargo test --workspace`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`

### 45. game_helper_core 开始接回 HelperMod 控制路径（2026-04-08）
- `game_helper_core` 已新增 C ABI：
  - `game_helper_core_evaluate_status_contract(...)`
  - `game_helper_core_evaluate_control_contract(...)`
  - `game_helper_core_decode_control_apply_plan(...)`
- `Payload.vcxproj` 当前已改为同时构建并链接：
  - `game_payload_core`
  - `game_helper_core`
- `HelperMod.cpp` 当前已改为：
  - `ControlReaderTick()` 直接消费 Rust 的 control 协议校验
  - `ApplyControlActions(...)` 直接消费 Rust 解码后的控制应用计划
- 当前已从 `HelperMod.cpp` 收口到 Rust 的部分包括：
  - control 协议契约校验
  - action mask 解码
  - 控制计划字段展开
- 本机已完成验证：
  - `cargo test -p game_helper_core`
  - `cargo clippy -p game_helper_core --all-targets --all-features -- -D warnings`
  - `cargo test --workspace`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  - `Payload.vcxproj` Debug|Win32 Build (`v142`)
  - `Payload.vcxproj` Release|Win32 Build (`v142`)

### 46. game_helper_core 开始接回 HelperStatus 快照生成（2026-04-08）
- `game_helper_core` 已新增：
  - `HelperStatusSnapshotInput`
  - `build_helper_status_snapshot(...)`
- `game_helper_core_ffi` 已新增：
  - `HelperStatusSnapshotInputInterop`
  - `game_helper_core_build_status_snapshot(...)`
- `HelperMod.cpp` 当前已改为：
  - `WriteSharedMemorySnapshot()` 直接消费 Rust 生成的 `HelperStatusV5` 数值快照主体
  - C++ 只保留 patch 状态探测、玩家名读取与最终写共享内存
- 当前已从 `HelperMod.cpp` 收口到 Rust 的状态侧内容包括：
  - control 协议校验
  - action mask 解码
  - status snapshot 主体生成
- 本机已完成验证：
  - `cargo test -p game_helper_core`
  - `cargo clippy -p game_helper_core --all-targets --all-features -- -D warnings`
  - `cargo test --workspace`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  - `Payload.vcxproj` Debug|Win32 Build (`v142`)
  - `Payload.vcxproj` Release|Win32 Build (`v142`)

### 47. game_control_core 开始接回 SyncController（2026-04-08）
- `DNFSyncBox.csproj` 当前已改为：
  - 构建前自动编译 `game_control_core`
  - 构建后复制 `game_control_core.dll`
- `SyncController.cs` 当前已改为直接消费 Rust 控制内核：
  - `RefreshWindows()` 的 foreground / grace / auto pause 判定
  - `PublishSnapshot()` 的共享快照头部构建
- `NativeMethods.cs` 已新增：
  - `game_control_core_evaluate_foreground(...)`
  - `game_control_core_build_publish_header(...)`
  - 对应 interop 结构
- 本机已完成验证：
  - `cargo test --workspace`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  - `DNFSyncBox.csproj` Debug / Release Build
  - `GameMasterGUI.csproj` Debug / Release Build
  - 输出目录已包含 `game_control_core.dll`

### 48. game_control_core 开始接回发布模式与 block mask 收口（2026-04-08）
- `game_control_core` 已新增：
  - `finalize_publish_profile(...)`
- `game_control_core_ffi` 已新增：
  - `game_control_core_finalize_publish_profile(...)`
- `SyncController.cs` 当前已改为直接消费 Rust 的：
  - `reportedMode` 计算
  - Replace -> Mapping 模式提升
  - mapping source mask 合并到 block mask
- 这一步意味着 `PublishSnapshot()` 中与“共享快照发布形态”直接相关的逻辑已进一步迁出 GUI
- 本机已完成验证：
  - `cargo test --workspace`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  - `DNFSyncBox.csproj` Debug / Release Build
  - `GameMasterGUI.csproj` Debug / Release Build

### 49. game_helper_core 开始接回 ControlReaderTick 运行时状态机（2026-04-08）
- `game_helper_core` 已新增：
  - `HelperControlRuntimeState`
  - `HelperControlTickDecision`
  - `normalize_control_value(...)`
  - `evaluate_control_tick(...)`
- `game_helper_core_ffi` 已新增：
  - `HelperControlRuntimeStateInterop`
  - `HelperControlTickDecisionInterop`
  - `game_helper_core_evaluate_control_tick(...)`
- `HelperMod.cpp` 当前已改为：
  - `ControlReaderTick()` 直接消费 Rust 的 control runtime decision
  - C++ 不再自己比较 control 前态或 sequence 变化
- 当前已从 `HelperMod.cpp` 收口到 Rust 的控制读取逻辑包括：
  - control 协议契约校验
  - action mask 解码
  - status snapshot 主体生成
  - control runtime state 比较与 sequence 触发判断
- 本机已完成验证：
  - `cargo test -p game_helper_core`
  - `cargo clippy -p game_helper_core --all-targets --all-features -- -D warnings`
  - `cargo test --workspace`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  - `Payload.vcxproj` Debug|Win32 Build (`v142`)
  - `Payload.vcxproj` Release|Win32 Build (`v142`)

### 50. game_control_core 开始接回输入掩码合并逻辑（2026-04-08）
- `game_control_core` 已新增：
  - `finalize_input_mask(...)`
- `game_control_core_ffi` 已新增：
  - `game_control_core_finalize_input_mask(...)`
- `SyncController.cs` 当前已改为：
  - `BuildInputMask()` 直接消费 Rust 的输入掩码合并逻辑
  - C# 不再自己逐项把 `mapping source mask` 合并到 `input_mask`
- 本机已完成验证：
  - `cargo test --workspace`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  - `DNFSyncBox.csproj` Debug / Release Build
  - `GameMasterGUI.csproj` Debug / Release Build

### 51. game_helper_core 开始接回 ApplyControlOverrides 决策（2026-04-08）
- `game_helper_core` 已新增：
  - `HelperOverridePlan`
  - `build_control_override_plan(...)`
- `game_helper_core_ffi` 已新增：
  - `HelperOverridePlanInterop`
  - `game_helper_core_build_control_override_plan(...)`
- `HelperMod.cpp` 当前已改为：
  - `ApplyControlOverrides()` 直接消费 Rust override plan
  - C++ 不再自己分支判断 hotkey / fullscreen attack / auto transparent / attract / gather / fullscreen skill 的最终覆盖结果
- 本机已完成验证：
  - `cargo test -p game_helper_core`
  - `cargo clippy -p game_helper_core --all-targets --all-features -- -D warnings`
  - `cargo test --workspace`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  - `Payload.vcxproj` Debug|Win32 Build (`v142`)
  - `Payload.vcxproj` Release|Win32 Build (`v142`)

### 52. game_control_core 开始接回 physical alignment 计划（2026-04-08）
- `game_control_core` 已新增：
  - `build_physical_alignment_plan(...)`
- `game_control_core_ffi` 已新增：
  - `game_control_core_build_physical_alignment_plan(...)`
- `SyncController.cs` 当前已改为：
  - `AlignKeyStateWithPhysicalInput()` 直接消费 Rust 生成的 `apply_mask / desired_down`
  - C# 不再自己决定补抬起/补按下的条件分支
- 本机已完成验证：
  - `cargo test --workspace`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  - `DNFSyncBox.csproj` Debug / Release Build
  - `GameMasterGUI.csproj` Debug / Release Build

### 53. game_control_core 开始接回 heartbeat 与 physical alignment 流程计划（2026-04-08）
- `game_control_core` 已新增：
  - `HeartbeatPlan`
  - `build_heartbeat_plan(...)`
- `game_control_core_ffi` 已新增：
  - `ControlHeartbeatPlanInterop`
  - `game_control_core_build_heartbeat_plan(...)`
- `SyncController.cs` 当前已改为：
  - `HeartbeatTick()` 直接消费 Rust heartbeat plan
  - `AlignKeyStateWithPhysicalInput()` 直接消费 Rust physical alignment plan
- 本机已完成验证：
  - `cargo test --workspace`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  - `DNFSyncBox.csproj` Debug / Release Build
  - `GameMasterGUI.csproj` Debug / Release Build

### 29. Sync state store 开始收口到 Rust（2026-04-07）
- `game_payload_core::sync` 已新增：
  - `ProjectedChannelKind`
  - `SyncStateStore`
- `game_payload_core_ffi` 已新增：
  - `payload_core_state_store_create(...)`
  - `payload_core_state_store_set/get_logical_desired(...)`
  - `payload_core_state_store_set/get_projected(...)`
  - `payload_core_state_store_clear_logical_desired(...)`
  - `payload_core_state_store_clear_all_projected(...)`
  - `payload_core_state_store_clear_projected_channel(...)`
- `SyncMod.cpp` 已开始改为：
  - Rust store 负责 `logical desired` / `raw projected` / `win32 projected` / `direct input projected`
  - C++ 四个数组退化为镜像缓存
- 当前已覆盖的关键路径：
  - clear
  - RawInput 部分 projected state 推进
  - logical desired 前态读取
  - Win32 / DirectInput projected state 推进
- 本机已完成验证：
  - `cargo test -p game_payload_core`
  - `cargo clippy -p game_payload_core --all-targets --all-features -- -D warnings`
  - `Payload.vcxproj` Debug|Win32 Build (`v142`)
  - `Payload.vcxproj` Release|Win32 Build (`v142`)
